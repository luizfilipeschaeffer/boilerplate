/** Espelha o enum Prisma `PlatformRole` — sem importar @boilerplate/db no Edge. */
export type PlatformRole =
  | "platform_admin"
  | "platform_comercial"
  | "platform_suporte"
  | "platform_produto"
  | "platform_engenharia";
