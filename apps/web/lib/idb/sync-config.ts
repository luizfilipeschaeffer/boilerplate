/** Intervalo padrão do sync em background (ms). */
export const SYNC_INTERVAL_MS = 30_000;

/** A cada N syncs incrementais, reconcilia IDs com o servidor (remove deletados). */
export const FULL_RECONCILE_EVERY = 5;
