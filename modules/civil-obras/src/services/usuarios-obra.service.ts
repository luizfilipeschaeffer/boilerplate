import type { ModuleServerContext } from "@boilerplate/sdk-server";

export function createUsuariosObraService(ctx: ModuleServerContext) {
  void ctx;
  return {
    /** Convites e auth isolada são orquestrados pelo host (`packages/db`). */
  };
}
