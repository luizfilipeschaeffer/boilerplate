import type { ModuleServerContext } from "@boilerplate/sdk-server";

export function createRelatorioService(ctx: ModuleServerContext) {
  void ctx;
  return {
    /** PDF gerado no host via `apps/web/lib/civil-obras-pdf.ts`. */
  };
}
