name: CI

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master, dev]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DATABASE_URL: postgresql://boilerplate:boilerplate@localhost:5432/boilerplate
  AUTH_SECRET: ci-only-secret-do-not-use-in-production
  NEXT_PUBLIC_APP_URL: http://localhost:3000

jobs:
  quality:
    name: Lint, validate e build
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: boilerplate
          POSTGRES_PASSWORD: boilerplate
          POSTGRES_DB: boilerplate
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U boilerplate -d boilerplate"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Environment file (apps com --env-file)
        run: |
          cat > .env <<'EOF'
          DATABASE_URL=postgresql://boilerplate:boilerplate@localhost:5432/boilerplate
          AUTH_SECRET=ci-only-secret-do-not-use-in-production
          INTEGRATOR_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
          EOF

      - name: Validate environment
        run: bun scripts/validate-env.ts

      - name: Dependency audit
        run: bun audit --audit-level=high

      - name: SDK tests
        run: bun run --filter @boilerplate/sdk-core test

      - name: Validate Prisma schema
        run: bun run db:validate

      - name: Lint
        run: bun run lint

      - name: Build (monorepo, inclui generate via @boilerplate/db#build)
        run: bun run build

      - name: Checagem estilo Vercel (clone limpo)
        run: bun run check:vercel -- --fresh --skip-install
